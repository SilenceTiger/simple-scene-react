import React = require('react');
import SimpleScene from '../../../src/index';

const Step1 = () => {
  return (
    <SimpleScene
      style={{
        background: '#000',
      }}
      showAxisHelper={true}
    />
  );
};

export default Step1;
